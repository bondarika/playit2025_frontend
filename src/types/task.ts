export interface Task {
  link: string;
  task: string;
  day: number;
  description: string;
  character: string;
  difficulty: string;
  points: number;
  verification: string;
  id: number;
  done: boolean;
  answer_format: string;
}
